# 學測英文寫作追蹤本

給單一學生使用的學測（GSAT）英文非選擇題（中譯英＋英文作文）寫作弱點追蹤工具。

## 檔案結構

```
index.html   ← 網頁本體，畫面與邏輯，不含任何學生資料
data.json    ← 所有資料（弱點紀錄、作品集）都在這裡
robots.txt   ← 禁止搜尋引擎爬取
README.md    ← 本文件
.gitignore
```

`index.html` 一載入就會 `fetch('./data.json')` 把資料抓進來渲染，兩個檔案必須放在同一層目錄。

## 四個分頁在做什麼

| 分頁 | 內容 | 排序依據 |
|---|---|---|
| 弱點追蹤 | 所有弱點條目，點擊有解說的卡片會跳出規則說明彈窗 | `date` 由新到舊 |
| 持續弱點總覽 | 同樣的條目，但用累計次數排序，看得出哪些是主要戰場 | `count` 由多到少 |
| 進步趨勢 | Chart.js 折線圖（分數 /20）＋各篇分數列表 | `date` 由舊到新 |
| 作品集 | 每篇作文，點標題展開原文／批改後版本／備註 | `date` 由新到舊 |

## how to 新增一篇新作文

1. 批改
2. Claude 把這篇整理成 `portfolio` 陣列的一筆，同時累加 `weak` 裡對應的弱點次數、必要時新增弱點類別
3. Claude 直接給你更新後的**完整** `data.json`
4. 把新的 `data.json` 上傳到 GitHub repo，**取代**舊的檔案（GitHub 網頁介面：進到 repo → 點 `data.json` → 右上角鉛筆圖示編輯，或用 Upload files 覆蓋上傳）
5. Commit 之後 Vercel **會自動重新部署**

## data.json 資料格式

```json
{
  "weak": [
    {
      "id": "w0",
      "cat": "類別名稱",
      "desc": "說明（顯示在卡片上）",
      "date": "YYYY-MM-DD",
      "count": 累計出現次數,
      "explain": { ... }
    }
  ],
  "portfolio": [
    {
      "id": "唯一英文代號",
      "title": "作文標題",
      "date": "YYYY-MM-DD",
      "score": 分數（0-20）,
      "prompt": "題目說明",
      "original": "學生原文",
      "corrected": "批改後版本",
      "note": "備註"
    }
  ]
}
```

### `date` 欄位的語意

`weak` 裡的 `date` 是「**首次記錄的日期**」，卡片上顯示為「首次記錄：」。**弱點復發時只累加 `count`，不要動 `date`**，否則舊弱點的歷史起點會被抹掉。

建立新類別時，`date` 要填「**這個錯誤第一次出現在作品裡的日期**」，而不是「今天想到要建這個類別的日期」。兩者常常差好幾個月——某個錯誤可能犯了很久才被歸納成一類。若日後補收較早的作文，也要回頭檢查是否有條目的 `date` 該往前挪。

### `explain` 欄位（選填，決定點擊卡片後彈窗的內容）

沒有 `explain` 的條目不可點擊。有兩種格式：

**A. 規則說明型**（多數條目用這種）

```json
"explain": {
  "rule": "為什麼會錯、怎麼想才對的說明文字",
  "examples": [
    "✗ 錯誤例句 → ✓ 正確例句",
    "✓ 正確用法示範"
  ]
}
```

**B. 字表型**（拼字、相似詞、詞性混淆這類需要條列對照的）

```json
"explain": {
  "type": "wordlist",
  "words": [
    { "wrong": "錯誤拼法", "correct": "正確拼法", "count": 次數, "note": "選填補充" }
  ]
}
```

字表型必須有 `"type": "wordlist"`，程式靠這個欄位判斷要用哪種版面；規則說明型則不需要 `type`。彈窗內的字表會自動依 `count` 由多到少排序。

## 隱私防護

- `robots.txt` 禁止搜尋引擎爬取整站
- `index.html` 的 `<head>` 內含 `<meta name="robots" content="noindex, nofollow, noarchive">`，即使被爬蟲讀到也不會被索引

## 已知問題 / 待辦

- `count` 的口徑已於 2026-09-06 統一為「**十篇作文中實際出現的處數**」（原本部分條目在數篇數、部分在數處數，無法互相比較）。日後累加時請沿用同一口徑：一篇裡犯三次就加 3，不是加 1
- `explain` 內字表（`wordlist`）的個別 `count` 只校正了可用字串比對驗證的項目，其餘沿用舊值，可能低估。字表次數與條目總 `count` 不必然相等
- 曾嘗試加入 `lastSeen`（最近出現日）與「已 N 篇未出現」標籤，但舊條目缺乏歷史資料只能填近似值，畫面數字不可信，已於 2026-09-06 移除。若日後從某個時間點開始確實逐次記錄，可再導入

## 版本紀錄

- 2026-08-23：資料與畫面分離為 `data.json` + `index.html`，方便日後只更新資料檔即可上線新內容，不需重寫整份程式碼
- 2026-09-06（二）：修掉 `index.html` 檔尾重複的 `</script>`。曾導入 `lastSeen` 新鮮度標籤，因缺乏歷史資料、數字不可信，同日移除
- 2026-09-06（四）：補收 2026-08-09 遺漏的作文（網路評論，8/20），作品集達 11 篇。新增弱點類別「動詞句型／搭配錯誤」（動詞後接錯結構，跨七篇 11 處），並自「動詞形式錯誤」「語意不明」「詞彙誤用」各移出 1 處避免重複計。同時依全文重掃結果修正 6 個條目的 `date`（原先記的是「類別被建立的日子」，非「第一次犯的日子」）
- 2026-09-06（三）：十篇作文全文重掃，所有 `count` 改以「實際出現處數」統一口徑重算（總計 361 處）。「複數 -s 遺漏」擴充為「單複數形式錯誤」（涵蓋 -s 誤加，如 peoples／On the other hands）；「助動詞後動詞形式錯誤」擴充為「動詞形式錯誤（助動詞／不定詞後）」（涵蓋 to got、to having transform）
- 2026-09-06：新增第 10 篇作品（寵物進入一般餐廳，看圖作文）。弱點分類調整：把「附屬子句片段（主要子句遺漏）」從「run-on / 逗號連接句」拆出獨立成條，兩者成因相反不宜混計；新增「複數 -s 遺漏」「助動詞後動詞形式錯誤」「冠詞遺漏（單數可數名詞）」三類。README 補上 `explain` 欄位格式與 `date` 語意說明
